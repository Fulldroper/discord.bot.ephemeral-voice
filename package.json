{
  "name": "new",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "clear && node .",
    "test": "nodemon --exec npm start",
    "worker": "npm i --omit=dev && node .",
    "logs": "heroku logs --tail",
    "deploy": "git add . && git commit -m 'upd' && git push heroku master && git push github master"
  },
  "config": {
    "path": {
      "events": "./events/",
      "command": "./command/",
      "db": "./db/"
    },
    "voicecouldown": 10000
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^16.0.1",
    "nodemon": "^2.0.16"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "discord.js": "^13.8.0",
    "fs": "^0.0.1-security",
    "redis": "^4.1.0"
  }
}
